apiVersion: v1
kind: Service
metadata:
  name: my-observability-grafana
spec:
  type: NodePort
  selector:
    app: my-observability-grafana
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 32000
